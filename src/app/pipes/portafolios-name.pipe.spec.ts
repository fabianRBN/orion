import { PortafoliosNamePipe } from './portafolios-name.pipe';

describe('PortafoliosNamePipe', () => {
  it('create an instance', () => {
    const pipe = new PortafoliosNamePipe();
    expect(pipe).toBeTruthy();
  });
});
